clients:
  hosts:
    living-room:
      ansible_host: living-room-audio.local
      ansible_user: snapcast
    kitchen:
      ansible_host: kitchen-audio.local
      ansible_user: snapcast
  children:
    speakers:
      hosts:
        passage:
          ansible_host: passage-audio.local
          ansible_user: snapcast
        bathroom:
          ansible_host: bathroom-audio.local
          ansible_user: snapcast

servers:
  hosts:
    living-room:
      ansible_host: living-room-audio.local
      ansible_user: snapcast
